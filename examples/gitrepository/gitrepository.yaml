apiVersion: gitrepository.azuredevops.crossplane.io/v1alpha1
kind: GitRepository
metadata:
  name: example-repository
  namespace: default
spec:
  providerConfigRef:
    name: example
  forProvider:
    name: example-repository
    projectIdRef:
      name: example-project
    defaultBranch: refs/heads/main
